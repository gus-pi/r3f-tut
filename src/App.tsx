import { Canvas } from '@react-three/fiber';

const App = () => {
    return (
        <div id="canvas-container" className="w-screen h-screen">
            <Canvas className="bg-sky-700"></Canvas>
        </div>
    );
};
export default App;
